Read the file at the path printed by `yaaml path` (run it as a shell command).
If the file exists and is non-empty, summarize the memories it contains for your context.
If it does not exist, note that no memories are available yet.
